const { writeCSV } = require("./csvWriter");

describe("CSV Writer", () => {
  test("Calls the write method", () => {});
  test("Given (Headers, Records, Filename) produces a CSV ", () => {});
  test("Produces a CSV at the proper location/directory", () => {});
  test("Throws an error if inputs are incorrect", () => {});
});
